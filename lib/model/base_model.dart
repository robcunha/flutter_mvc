class BaseModel {
    /// Create a instance of a class
    create(){}

    /// Find a resource from a database via api call
    find(id){}

    ///Retrive all data from a database via api call
    all(){}

    ///save a resource from database via api call
    save(){}

    ///Update a resource from database via api call
    update(id){}

    ///Destroy a resource from database via api call
    destroy(id){}

    ///Count the number of resources retrieves form a database via api call
    count(){}
}
